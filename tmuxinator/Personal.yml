# /Users/pablobfonseca/.config/tmuxinator/Personal.yml

name: Personal
root: ~/

windows:
  - dotfiles:
      layout: main-vertical
      root: ~/.dotfiles
  - code:
      layout: main-vertical
      root: ~/code
      panes:
        - claudeos
